# 테스트 가이드 (Testing Guide)

Grid Tile Editor의 자동 테스트 실행 방법과 브라우저에서 직접 확인하는 수동 UI 테스트 절차입니다.
기능을 수정했다면 관련 시나리오를 함께 갱신해 주세요.

> 개요·설치·데이터 형식은 [README.md](./README.md)를 참고하세요.

---

## 자동 테스트

```bash
npm test
```

| 파일 | 검증 대상 |
|---|---|
| `tests/rendered-html.test.mjs` | 서버 렌더 결과 HTML |
| `tests/range-clipboard.test.mjs` | 범위 선택 · 복사/잘라내기/붙여넣기 |
| `tests/multipage.test.mjs` | 페이지 CRUD · 페이지 간 독립성 |
| `tests/render-legend.test.mjs` | 범례 — 페이지별 사용 항목 · PNG 렌더 |
| `tests/cell-text.test.mjs` | 칸 글자 — 줄이기 · 두 줄 · 가로 압축 · 말줄임 |
| `tests/zoom.test.mjs` | 휠 확대 시 커서 고정 계산 |
| `tests/paper.test.mjs` | 용지 규격 · 장수 계산 |
| `tests/mcp-tools.test.mjs` | MCP 도구 8종 · 프로젝트 파일 저장소 |
| `tests/pattern.test.mjs` | 채움 무늬 · 선 모양 렌더 · 저장 · 하위 호환 |
| `tests/palette-collapse.test.mjs` | 팔레트 분류 접기 상태 · 브라우저 저장 |
| `tests/print-sheet.test.mjs` | 용지 규격 PNG — 이미지 크기 · 여백 · 장 나누기 |
| `tests/ruler-watermark.test.mjs` | 눈금자 눈금·강조 · 워터마크 한 줄 |
| `tests/diff-photo.test.mjs` | 리비전 비교 · MCP 이력 도구 · 칸 사진 |
| `tests/photo-ledger.test.mjs` | 사진 대장 — 모으기 순서 · 파일 이름 · 인쇄용 HTML |
| `tests/projects-api.test.mjs` | 로컬 폴더 공유 — 리비전 · 충돌 · 복원 · HTTP 라우팅 |

---

## 수동 UI 테스트 절차

UI 동작은 브라우저에서 아래 순서로 직접 확인합니다.

<details>
<summary><b>[1] 격자 드래그 칠하기와 도구</b></summary>

1. 편집기를 엽니다 (초기 예시 도면이 뜨는지 확인).
2. 왼쪽 팔레트에서 `배경 > 벽`을 클릭합니다.
3. `브러시`가 선택된 상태에서 빈 격자를 드래그 → 벽 타일이 연속으로 칠해집니다.
4. `직선` 도구로 한 칸에서 다른 칸까지 드래그 → 직선으로 그어집니다.
5. `지우개`로 칠해진 영역을 지웁니다.
</details>

<details>
<summary><b>[2] 팔레트 중첩과 레이어 토글</b></summary>

1. `상태 > 설치 (정상)`(초록)을 빈 칸에 칠합니다.
2. `장비 > 리더`를 고른 뒤 방금 칠한 셀을 클릭 → 초록 배경 위에 `리더` 글자가 올라갑니다.
3. `배선 > 신호선`으로 설비 사이를 드래그해 연결합니다.
4. 왼쪽 패널에서 `배선` 레이어의 `표시`를 해제 → 배선 라인만 깔끔하게 사라집니다.
</details>

<details>
<summary><b>[3] 사용자 정의 팔레트와 보존 정책</b></summary>

1. `상태` 섹션의 `+ 상태 추가`를 누릅니다.
2. 색을 핑크로 바꾸고 이름 `교체 예정`, 설명 `3월 교체 예정 설비`를 넣고 `추가` → 즉시 선택되어 칠할 수 있습니다.
3. `편집`으로 이름을 `교체 필요`로 고치고 `저장`합니다.
4. 그 상태로 몇 칸 칠한 뒤 `삭제` → `칸은 그대로 두고 목록에서만 삭제` / `배치된 N칸까지 함께 삭제` 두 선택지가 뜹니다.
5. 앞쪽을 누르면 목록에서만 사라지고 **칠해진 핑크색은 그대로 남습니다.**
</details>

<details>
<summary><b>[3-1] 디스플레이 이름과 설명</b></summary>

1. `장비 > 리더`의 `편집` → 이름 `리더`, 설명 `식별 리더기`가 들어 있습니다.
2. 이름을 `RD-1`로 바꾸고 저장 → 팔레트·범례는 물론 **이미 배치된 도면 칸의 글자도** 바뀝니다.
3. `PNG 저장 (현재 페이지)` → 범례에 `RD-1 — 식별 리더기` 처럼 설명이 옅은 글씨로 붙습니다.
4. 설명을 지우고 다시 PNG → 이름만 남고 `—` 구분선이 남지 않아야 합니다.
</details>

<details>
<summary><b>[3-2] 칸 메모 — 우클릭 편집과 말풍선</b></summary>

1. `브러시`를 고른 채 칸에서 **우클릭** → `칸 정보` 상자가 열리고 `장비 ID`에 커서가 잡힙니다. **우클릭한 칸이 칠해지면 안 됩니다.**
2. `C1101` / `3월 점검 대상`을 넣고 `Enter` → 칸 가운데에 `C1101`, 오른쪽 위에 점 표시.
3. 그 칸에 마우스를 올리면 말풍선이 뜹니다. 도면 오른쪽·아래 끝에서는 안쪽으로 방향이 바뀝니다.
4. 상태색·장비가 있는 칸에 우클릭하면 상자 위에 `가로 N · 세로 M · 상태 · 장비`가 보이고, 저장 후에도 색과 장비가 남습니다.
5. `지우기` → 장비 ID·메모와 점 표시만 사라집니다.
6. `Esc` 또는 바깥 클릭으로 닫힙니다.
7. 페이지를 추가해 같은 좌표에 다른 메모 → 페이지마다 따로 저장됩니다.
</details>

<details>
<summary><b>[3-3] 휠 확대·축소</b></summary>

1. 특정 칸에 커서를 두고 휠을 위로 → 배율이 오르고 **그 칸이 커서 밑에서 움직이지 않아야** 합니다.
2. 아래로 굴려 원래 배율로 → 마찬가지.
3. 왼쪽 위 끝 칸에서 최대 축소 → 도면이 잘리거나 화면 밖으로 나가지 않아야 합니다.
4. 상단 `확대`/`축소` 버튼도 같은 동작.
5. 도면 위 휠은 **브라우저 확대가 아니라** 도면 확대에만 쓰여야 합니다.
</details>

<details>
<summary><b>[3-4] 가운데 버튼 화면 이동</b></summary>

1. 격자를 `120 x 80` 정도로 키워 도면이 화면보다 크게 만듭니다.
2. **가운데 버튼(휠 클릭)** 을 누른 채 드래그 → 도면이 따라 움직이고 커서가 `grabbing`으로 바뀝니다.
3. 버튼을 놓으면 커서 복귀. 이동 중에 **칸이 칠해지면 안 됩니다.**
4. 윈도우의 자동 스크롤 아이콘이 뜨지 않아야 합니다.
</details>

<details>
<summary><b>[3-5] 인쇄 용지 경계선</b></summary>

1. `인쇄 용지 > 인쇄 경계선 켜기` → 기본값(A4 가로 · 5mm · 여백 10mm)과 `한 장에 가로 55 × 세로 38칸` 안내가 뜹니다. 격자(48 × 30)가 한 장보다 작으므로 오른쪽·아래에 **회색 여유 영역**이 생기고 바깥에 자홍색 점선이 보입니다.
2. 격자를 `120 x 80`으로 바꾸면 점선이 세로 1줄·가로 2줄로 나타나고 안내가 `가로 3 · 세로 3 = 9장`으로 바뀝니다.
3. `한 칸 (mm)`을 `10`으로 올리면 점선이 촘촘해집니다.
4. `방향`을 `세로`로 바꾸면 가로·세로 간격이 서로 바뀝니다.
5. 격자 바로 아래에 **범례가 미리 그려지고** 경계선이 그 범례까지 감쌉니다. 안내도 `+ 범례 2행`을 함께 보여 줍니다.
6. 팔레트 항목을 늘리면 범례 띠가 두꺼워지고 경계선·장수가 따라 바뀝니다.
7. `PNG 저장` → **PNG에는 점선이 없어야** 합니다(범례는 들어갑니다).
8. 새 페이지는 경계선이 꺼진 상태로 시작합니다. 새로고침 후에도 설정은 남아 있습니다.
</details>

<details>
<summary><b>[4] 인스펙터 · [5] Undo/Redo · [6] 내보내기</b></summary>

1. `선택` 도구로 셀을 클릭하고 오른쪽 `선택한 칸`에서 장비 ID `C1199`, 메모를 입력한 뒤 입력창 밖 클릭 → 셀 중앙에 ID, 오른쪽 위에 메모 점이 뜹니다.
2. `Ctrl + Z` / `Ctrl + Y`로 직전 작업이 취소·복원되는지 확인합니다.
3. `PNG 저장` → 제목 · 도면 · 범례가 모두 렌더된 이미지가 내려받아집니다.
4. `JSON 내보내기` → 커스텀 팔레트가 포함된 `.json` 파일.
5. `전체 초기화` → 확인 창을 거쳐 격자가 비고 localStorage가 지워집니다. **팔레트 정의는 남습니다.**
6. `예시 다시 넣기` → 초기 샘플 도면이 다시 로드됩니다.
</details>

<details>
<summary><b>[7] 범위 선택과 클립보드</b></summary>

1. `선택` 도구로 3 × 2 범위를 드래그 → 점선 테두리 + 반투명 하늘색 오버레이, `3 × 2` 배지, 오른쪽에 `선택한 범위 (가로 3 × 세로 2 셀)`.
2. `복사`(`Ctrl + C`) → 배경·설비(상태·장비·ID·메모)·배선이 함께 복사됩니다.
3. 원점 칸을 클릭하고 `붙여넣기`(`Ctrl + V`) → 상대 위치를 유지한 채 주입되고, 붙여넣은 영역이 자동 선택됩니다.
4. `잘라내기`(`Ctrl + X`) → 원본 위치의 모든 레이어와 메타가 제거됩니다.
5. `Ctrl + Z` 한 번으로 이전 상태로 원복됩니다.
6. 텍스트 입력창에 커서가 있을 때는 일반 텍스트 복사/붙여넣기가 정상 동작해야 합니다.
</details>

<details>
<summary><b>[8] 다중 페이지</b></summary>

1. `+ 페이지 추가` → 빈 페이지가 추가되고 자동 전환됩니다. 가운데 페이지를 지웠다 다시 추가해도 이름·ID가 겹치지 않습니다.
2. ✏️ 또는 더블클릭으로 이름을 `2공장 출하 라인`으로 바꿉니다.
3. 두 페이지를 오가며 셀·격자 크기·메모·배선이 **완전히 독립**인지 확인합니다.
4. `×` → 삭제 확인 팝업 후 삭제, 첫 페이지로 전환됩니다.
5. 마지막 1개 페이지의 `×`는 비활성입니다.
6. `JSON 내보내기` → 모든 페이지 + 공용 팔레트가 담깁니다.
7. `PNG 저장 (현재 페이지)` → 현재 페이지만. 범례는 공용 팔레트 기준입니다.
8. 페이지를 전환한 뒤 `Ctrl + Z` → 보고 있던 페이지는 그대로이고 **직전 편집 내용**이 취소됩니다.
</details>

<details>
<summary><b>[9] 페이지를 넘나드는 팔레트 삭제</b></summary>

1. 1페이지에서 `상태 > 설치 (정상)`으로 몇 칸 칠합니다.
2. 새 페이지를 만들어 이동합니다(비어 있음).
3. `설치 (정상)`의 `삭제` → 현재 페이지가 비어 있어도 **"도면 N칸에서 쓰이고 있다"** 라고 안내됩니다(다른 페이지까지 셈).
4. `칸은 그대로 두고 목록에서만 삭제` 후 1페이지로 복귀 → 칠해 둔 칸이 **원래 색 그대로**, 범례에도 남아 있어야 합니다.
5. `Ctrl + Z`로 되돌린 뒤 `배치된 N칸까지 함께 삭제` → **모든 페이지**에서 그 색 칸이 비워지고, 장비 ID·메모는 남습니다.
</details>


<details>
<summary><b>[10] 채움 무늬와 선 모양</b></summary>

1. `상태` 섹션의 `+ 상태 추가` → 추천 색에서 빨강을 고르고 `채움 무늬`에서 `빗금`을 누릅니다.
   * **결과**: 무늬 버튼의 견본이 실제 빗금으로 보이고, 추가한 항목의 팔레트 견본도 빗금으로 보입니다.
2. 그 항목으로 격자 몇 칸을 칠합니다.
   * **결과**: 칸이 옅은 빨강 바탕 위에 진한 빗금으로 그려지고, **빗금이 이웃 칸으로 넘치지 않아야** 합니다.
3. 같은 항목을 `편집`해 `교차빗금` · `점`으로 바꿔 봅니다.
   * **결과**: 이미 칠해 둔 칸이 그 자리에서 바로 바뀝니다.
4. `장비 > 리더`를 편집해 `선 모양`을 `파선`으로 바꿉니다.
   * **결과**: 도면에서 장비 칸 테두리가 파선으로 바뀌고, 오른쪽 `범례`의 견본 테두리도 파선이 됩니다.
5. `배선` 항목을 편집합니다.
   * **결과**: 배선에는 `채움 무늬` 선택기가 없고 `선 모양`만 있습니다. 팔레트 견본도 칸 채움이 아니라 가로지르는 선으로 보입니다.
6. 선 모양을 `점선`으로 바꾸고 배선을 몇 칸 이어 그립니다.
   * **결과**: 이어진 경로가 점선으로 그려집니다. 홀로 떨어진 칸은 가운데 점만 남습니다.
7. 배선을 `ㄱ` 자로 꺾어 그리고 점선 상태에서 확대해 봅니다.
   * **결과**: 이웃 칸으로 넘어갈 때 점 간격이 이어지고, 꺾이는 칸에는 매듭이 채워져 끊겨 보이지 않습니다.
8. 밝은 노랑(`미연결 (점검)`) 칸과 빗금 칸에 장비 ID를 넣어 봅니다.
   * **결과**: 글자 둘레에 반대색 테두리가 둘러져 바탕 무늬에 묻히지 않습니다.
9. `PNG 저장 (현재 페이지)` 로 이미지를 확인합니다.
   * **결과**: 도면과 범례 모두 화면과 같은 무늬 · 선 모양으로 나옵니다. 범례의 배선 항목은 가로지르는 선입니다.
10. 무늬를 `솔리드`, 선 모양을 `실선`으로 되돌립니다.
   * **결과**: 예전과 똑같은 통 채움 · 실선으로 돌아옵니다.
11. `JSON 내보내기` 후 다시 `불러오기` 합니다.
   * **결과**: 무늬와 선 모양이 그대로 남아 있습니다. 무늬를 한 번도 건드리지 않은 예전 JSON 도 그대로 열립니다.
</details>

<details>
<summary><b>[11] 용지 규격에 맞춘 PNG 저장</b></summary>

1. `인쇄 용지`를 켜고 `A4` · `세로` · `한 칸 5mm` · `여백 10mm`로 둡니다. 격자는 `30 × 30`.
   * **결과**: 안내가 `한 장에 가로 38 × 세로 55칸`, `… = 1장`으로 보이고, 그 아래에 `PNG 저장은 이 용지 규격(150dpi)으로 뽑습니다`가 보입니다.
2. `PNG 저장 (현재 페이지)`를 누르고 내려받은 파일의 픽셀 크기를 봅니다.
   * **결과**: **1240 × 1754px** (A4 210 × 297mm @150dpi). 파일 이름 끝이 `-A4-세로-150dpi.png` 입니다.
3. 그 이미지를 이미지 뷰어에서 **100% 배율로 A4에 인쇄**합니다.
   * **결과**: 도면 둘레에 10mm 여백이 남고, 한 칸이 자로 재서 5mm 입니다.
   * **결과**: 도면 아래에 범례가 화면 미리보기와 같은 자리에 실립니다.
4. `방향`을 `가로`로 바꾸고 다시 저장합니다.
   * **결과**: 이미지가 1754 × 1240px 로 바뀝니다.
5. 격자를 `120 × 80`으로 키웁니다.
   * **결과**: 안내가 `가로 4 · 세로 2 = 8장` 처럼 바뀝니다.
6. 그 상태로 `PNG 저장`을 누릅니다.
   * **결과**: 안내한 장수만큼 파일이 내려받아지고(`-1of8` … `-8of8`), 파일마다 도면의 다른 조각이 왼쪽 위에 붙어 있습니다.
   * **결과**: 각 장 아래 여백에 `제목 · 3/8 (가로 3 · 세로 1) · 출력시각` 같은 워터마크가 있습니다.
   * **결과**: 어느 장에도 **자홍색 점선은 없어야** 합니다.
7. `인쇄 경계선 끄기`를 누르고 다시 `PNG 저장`을 누릅니다.
   * **결과**: 예전처럼 제목 + 도면 + 범례가 격자 크기에 맞춰 한 장으로 나옵니다.
</details>

<details>
<summary><b>[12] 로컬 폴더 공유와 버전 이력</b></summary>

`npm run dev` 로 띄운 개발 서버에서만 확인할 수 있습니다(정적 배포본에는 서버가 없습니다).

1. 편집기를 엽니다.
   * **결과**: 도구 막대 아래에 `서버 도면` 줄이 보입니다. 저장된 도면이 없으면 `저장된 도면 없음` 입니다.
2. `작성자` 에 이름을 넣고 `새 도면` 을 눌러 이름을 정합니다.
   * **결과**: 목록에 새 도면이 생기고 오른쪽에 `<id> · r1` 이 보입니다. 프로젝트 폴더에 `.grid-projects/<id>.json` 이 생깁니다.
3. 칸 몇 개를 칠하고 `서버 저장` 을 누릅니다.
   * **결과**: `저장 완료 r2` 가 뜨고 `.grid-projects/.history/<id>/` 에 스냅샷이 2개 쌓입니다.
4. 브라우저 탭을 하나 더 열어 같은 도면을 엽니다. 두 탭에서 각각 다르게 칠합니다.
5. 두 번째 탭에서 먼저 저장한 뒤, 첫 번째 탭에서 `서버 저장` 을 누릅니다.
   * **결과**: 노란 띠로 `… 님이 먼저 저장했습니다(리비전 N)` 이 뜨고 `덮어쓰기` · `사본으로 저장` · `서버 것 열기` 를 고를 수 있습니다.
   * **결과**: 이때 서버 내용은 아직 바뀌지 않아야 합니다.
6. `사본으로 저장` 을 누릅니다.
   * **결과**: 새 ID 로 도면이 하나 더 생기고, 원본은 그대로 남습니다.
7. `버전 이력` 을 누릅니다.
   * **결과**: 저장 시각 · 작성자 · 제목 · 페이지 수가 최신 순으로 보입니다.
8. 과거 리비전의 `되돌리기` 를 누릅니다.
   * **결과**: 그 시점 내용으로 도면이 바뀌고, 이력은 지워지지 않은 채 새 리비전이 하나 더 쌓입니다(작성자에 `r<N> 복원` 표시).
9. MCP 서버(`npm run mcp:dev`)로 같은 도면을 고친 뒤 편집기에서 `서버 저장` 을 누릅니다.
   * **결과**: `편집기 밖에서 바뀌었습니다` 안내가 뜹니다. 같은 폴더를 함께 보고 있다는 뜻입니다.
10. `인쇄 용지` 를 A3 세로로 잡고 `서버 저장` → 새로고침 → 같은 도면을 다시 엽니다.
   * **결과**: 용지·방향·한 칸(mm)·여백이 그대로 살아 있습니다. `.grid-projects/<id>.json` 과 이력 스냅샷에도 `paper` 가 적혀 있습니다.
11. 개발 서버를 끈 채 `서버 저장` 을 눌러 봅니다.
   * **결과**: `Unexpected token '<'` 같은 오류 대신, 서버 도면 줄이 **노란 띠로 바뀌어** 이유와 `다시 연결` 버튼을 보여 줍니다. 줄이 사라지지는 않습니다.
   * **결과**: 개발 서버를 다시 켜고 `다시 연결` 을 누르면 목록이 돌아옵니다. 그동안에도 브라우저 자동 저장과 JSON 내보내기는 그대로 됩니다.
</details>

<details>
<summary><b>[13] 팔레트 분류 접기·펼치기</b></summary>

1. 왼쪽 팔레트의 `장비` 제목을 봅니다.
   * **결과**: 제목 앞에 `▼`, 뒤에 항목 수(`장비 (5)`)가 보입니다.
2. `장비` 제목을 누릅니다.
   * **결과**: 목록이 접히고 표시가 `▶` 로 바뀝니다. `+ 장비 추가` 버튼은 그대로 남습니다.
3. `장비` 항목 하나를 고른 뒤 다시 접습니다.
   * **결과**: 접힌 상태에서도 **지금 고른 항목 한 줄**이 남아 무엇으로 칠하는 중인지 보입니다. 그 줄을 누르면 다시 펼쳐집니다.
4. 접은 채로 `+ 장비 추가` 를 누릅니다.
   * **결과**: 입력 칸을 볼 수 있도록 분류가 자동으로 펼쳐집니다.
5. `상태` · `배선` · `배경` 도 각각 접어 봅니다.
   * **결과**: 분류마다 따로 접히고, 레이어 `표시` 체크박스와는 별개로 동작합니다(접어도 도면은 그대로 보입니다).
6. 새로고침(F5) 합니다.
   * **결과**: 접어 둔 분류가 그대로 접혀 있습니다.
</details>

<details>
<summary><b>[14] 범례와 칸 글자</b></summary>

1. 새 페이지를 만들고 상태 하나로 몇 칸만 칠합니다.
   * **결과**: 오른쪽 `범례` 에 **그 항목만** 보입니다. 팔레트에만 있고 쓰지 않은 항목은 나오지 않습니다.
2. 페이지를 오가며 범례를 봅니다.
   * **결과**: 페이지마다 쓴 항목이 달라 범례도 달라집니다.
3. `인쇄 용지` 를 켠 상태에서 항목을 하나 더 써 봅니다.
   * **결과**: 범례 띠와 장수 안내가 늘어난 항목만큼만 바뀝니다.
4. 장비 이름을 한글 네 글자(예: `훈련저장`)로 바꿉니다.
   * **결과**: 칸 안에서 **두 줄로 나뉘어** 들어가고, 좌우가 잘리지 않습니다.
5. 그 칸에 장비 ID도 함께 넣습니다.
   * **결과**: 위아래로 나눠 쓰므로 이름은 한 줄로 줄어들고, 그래도 칸을 넘지 않습니다.
6. 장비 이름을 아주 길게(10글자 이상) 바꿔 봅니다.
   * **결과**: 뒤가 `…` 로 접힙니다. 전체 이름은 팔레트와 범례에서 볼 수 있습니다.
7. 배율을 최소(14px)로 줄여 봅니다.
   * **결과**: 글자가 칸 밖으로 삐져나오지 않습니다.
</details>

<details>
<summary><b>[15] 눈금자 · 사진 첨부 · 워터마크</b></summary>

1. 도구 막대에서 `눈금자` 가 켜져 있는지 봅니다.
   * **결과**: 도면 위와 왼쪽에 칸 번호(0, 5, 10 …)가 붙어 있고, 스크롤하면 함께 따라옵니다.
2. 도면 위에서 마우스를 움직입니다.
   * **결과**: 커서가 놓인 열과 행이 눈금자에서 밝게 칠해지고 그 번호가 진해집니다.
3. 배율을 최소(14px)로 줄입니다.
   * **결과**: 숫자가 겹치지 않도록 간격이 넓어집니다(5칸 → 10칸 등).
4. `눈금자` 체크박스를 끕니다.
   * **결과**: 눈금자가 사라지고 도면이 그 자리를 차지합니다.
5. 칸을 우클릭해 `+ 사진 붙이기 (끌어다 놓기 가능)` 로 사진을 **여러 장 함께** 고릅니다.
   * **결과**: 상자 안에 썸네일이 3열 격자로 늘어서고 각 썸네일 아래에 그 장의 크기(예: `38KB`), 머리줄 오른쪽에 `3/8장 · 합계 112KB` 가 보입니다. 원본이 몇 MB 여도 이 크기로 줄어듭니다.
6. 파일 탐색기에서 사진 몇 장을 골라 상자의 사진 자리로 끌어다 놓습니다.
   * **결과**: 끌고 오는 동안 `+ 사진 붙이기` 단추 테두리가 진해지고, 놓으면 기존 목록 뒤에 더해집니다.
7. `저장` 후 그 칸을 봅니다.
   * **결과**: 칸 왼쪽 아래에 작은 네모 표시가 생기고 그 오른쪽에 장수(`3`)가 붙습니다. 마우스를 올리면 첫 장이 크게, 나머지가 작은 썸네일 줄로, 그 아래 `사진 3장` 이 말풍선에 뜹니다.
8. 다시 우클릭해 썸네일 오른쪽 위 `×` 로 한 장만 지우고 `저장`.
   * **결과**: 고른 장만 빠지고 나머지는 순서대로 남습니다. 칸의 장수 표시도 하나 줄어듭니다.
9. 남은 사진을 모두 지우고 `저장`.
   * **결과**: 표시가 사라집니다. 장비 ID·메모는 그대로 남습니다.
10. 사진 8장을 채웁니다.
   * **결과**: 단추가 `사진 8장 (가득 찼습니다)` 로 바뀌고 눌리지 않습니다. 더 끌어다 놓으면 `사진은 한 칸에 8장까지 붙일 수 있습니다.` 안내가 뜹니다.
11. 이미 붙어 있는 사진과 같은 파일을 다시 고릅니다.
   * **결과**: `같은 사진이 이미 붙어 있습니다.` 안내가 뜨고 목록은 그대로입니다.
12. 그림이 아닌 파일(예: `.txt`)을 골라 봅니다.
   * **결과**: `그림 파일만 붙일 수 있습니다.` 안내가 뜨고 아무것도 붙지 않습니다.
13. (있으면) 사진 한 장이 붙어 있던 **이전 판 도면**을 불러옵니다.
   * **결과**: 그 사진이 목록의 첫 장으로 그대로 보입니다.
14. `서버 저장` 으로 리비전을 올린 뒤 `PNG 저장` 을 누릅니다.
   * **결과**: 이미지 아래 여백에 `제목 · r3 · 2026-08-19 14:05 · 작성자: 홍길동` 한 줄이 옅게 찍힙니다.
15. 여러 장으로 나뉘는 도면에서 저장합니다.
   * **결과**: 워터마크에 `3/8 (가로 3 · 세로 1)` 처럼 몇 번째 장인지도 함께 적힙니다.

</details>

<details>
<summary><b>[16] 사진 대장 인쇄 · 사진 다운로드</b></summary>

사진이 붙은 칸을 두 개 이상 만들어 두고 시작합니다(페이지도 둘 이상이면 더 좋습니다).

1. 칸을 우클릭해 썸네일 오른쪽 위 `↓` 를 누릅니다.
   * **결과**: 그 사진 한 장이 `1층-메인-공장_5_3_C1101_photo_2.jpg` 형태(페이지_세로_가로_장비ID_photo_순번)로 저장됩니다. 장비 ID 를 비워 둔 칸은 그 자리가 `-` 입니다.
2. 사진을 새로 한 장 붙이고 **저장하지 않은 채** 그 사진의 `↓` 를 누릅니다.
   * **결과**: 방금 붙인 사진도 그대로 저장됩니다.
3. 같은 상자에서 `이 칸 사진 인쇄 (N장)` 를 누릅니다.
   * **결과**: 새 창에 그 칸의 사진만 모인 대장이 뜨고 인쇄 창이 함께 열립니다. 머리에 `{장비 ID} 칸 사진`, 그 아래 페이지 이름 · `가로 3 · 세로 5` · 장수가 적혀 있습니다.
4. 인쇄 미리보기에서 용지를 봅니다.
   * **결과**: A4 세로, 사진이 3열로 놓이고 사진과 그 설명(좌표 · 장비 ID · 메모)이 장 경계에서 갈리지 않습니다.
5. 도구 막대 오른쪽 `사진 대장 인쇄 (N장)` 을 누릅니다.
   * **결과**: 프로젝트 전체 사진이 좌표 순(위→아래, 왼→오른쪽)으로 모인 대장이 뜹니다. 머리에 `{배치도 이름} 사진 대장` 과 `사진 N장 · r{리비전} · {작성자} · {뽑은 시각}` 이 적혀 있습니다.
6. 페이지가 둘 이상인 프로젝트에서 같은 단추를 누릅니다.
   * **결과**: 페이지 이름별로 `2층 (3장)` 같은 머리줄로 갈라져 있습니다.
7. 팝업 차단을 켠 상태로 눌러 봅니다.
   * **결과**: `팝업이 막혀 사진 대장을 열지 못했습니다.` 안내가 뜹니다.
8. `사진 일괄 저장` 을 누릅니다.
   * **결과**: 장수를 알리는 확인창이 먼저 뜨고, 허용하면 사진이 하나씩 차례로 저장됩니다. 브라우저가 "여러 파일 저장" 을 물으면 허용해야 전부 저장됩니다.
9. 사진이 하나도 없는 새 프로젝트에서 두 단추를 봅니다.
   * **결과**: 둘 다 잠겨 있고, 가리키면 `칸에 붙은 사진이 없습니다` 라고 알려 줍니다.
</details>
